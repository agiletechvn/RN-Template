import React from 'react';
import Box from '@src/components/Box';
import AnimatedSlideButton from '@src/components/AnimatedSlideButton';

const AnimatedButtonWithSvgIconSample = () => {
  return (
    <Box flex={1} padding={[4]} background="white">
      <AnimatedSlideButton />
    </Box>
  );
};

export default AnimatedButtonWithSvgIconSample;
